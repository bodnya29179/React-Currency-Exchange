import { CURRENCIES } from './currencies';
import { CURRENCY_RATES } from './currency-rates';
import { DECIMAL_SEPARATOR, NAVIGATION_KEYS} from './keyboard-keys';

export {
  CURRENCIES,
  DECIMAL_SEPARATOR,
  NAVIGATION_KEYS,
  CURRENCY_RATES,
};
